-- SELECT DEPARTMENTS.NAME
-- FROM DEPARTMENTS
-- WHERE DEPARTMENTS.ID NOT IN (SELECT EMPLOYEES.DEPID
--                              FROM EMPLOYEES);

SELECT DEPARTMENTS.NAME
FROM DEPARTMENTS
  LEFT JOIN EMPLOYEES ON DEPARTMENTS.ID = EMPLOYEES.DEPID
WHERE EMPLOYEES.DEPID IS NULL;